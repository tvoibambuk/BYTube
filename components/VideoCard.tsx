import React from 'react'\nimport Link from 'next/link'\n\ntype Video = {\n  id: string\n  title: string\n  channel: string\n  views: number\n  created_at: string\n  path: string\n}\n\nexport default function VideoCard({ video }: { video: Video }) {\n  return (\n    <article className="card flex gap-3">\n      <Link href={`/watch/${video.id}`}>\n        <a className="w-2/5 bg-[#081022] min-h-[90px] rounded-lg flex items-center justify-center text-sm">🎬 Превью</a>\n      </Link>\n      <div className="flex-1">\n        <Link href={`/watch/${video.id}`}><a className="font-semibold text-white block">{video.title}</a></Link>\n        <div className="text-sm text-[#94a3b8]">{video.channel}</div>\n        <div className="text-xs text-[#94a3b8]">{video.views} • {new Date(video.created_at).toLocaleDateString()}</div>\n      </div>\n    </article>\n  )\n}